build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - python manage.py migrate --noinput
    - gunicorn accounting.wsgi:application --log-level info --timeout 180  --web 3
 
 
 
 
    # - gunicorn accounting.wsgi:application --bind 0.0.0.0=$PORT --log-level info --timeout 180  --web 3
